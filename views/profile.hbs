<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile - the blog site</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@100..900&family=Space+Grotesk:wght@300..700&display=swap" rel="stylesheet">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Gloock&display=swap" rel="stylesheet">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Geist:wght@100..900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/header.css">
    <link rel="stylesheet" href="/css/footer.css">
    <link rel="stylesheet" href="/css/profile.css">
</head>
<body>
    {{> header}}

    <main>
        <div class="profile-container">
            <div class="profile-info">
                <div class="profile-card">
                    <div class="profile-header">
                        <h2>User Profile</h2>
                    </div>
                    <div class="profile-details">
                        <div class="profile-item">
                            <span class="profile-label">Username:</span>
                            <span class="profile-value">{{user.username}}</span>
                        </div>
                        <div class="profile-item">
                            <span class="profile-label">First Name:</span>
                            <span class="profile-value">{{user.firstName}}</span>
                        </div>
                        <div class="profile-item">
                            <span class="profile-label">Last Name:</span>
                            <span class="profile-value">{{user.lastName}}</span>
                        </div>
                        <div class="profile-item">
                            <span class="profile-label">Email:</span>
                            <span class="profile-value">{{user.email}}</span>
                        </div>
                    </div>
                </div>
            </div>
            
            <div class="user-blogs">
                <h2>My Blogs</h2>
                
                {{#if blogs.length}}
                    {{#each blogs}}
                    <div class="blog-item">
                        <div class="blog-image">
                            <img src="{{this.imageUrl}}" alt="{{this.title}}" />
                        </div>
                        <div class="blog-content">
                            <h3 class="blog-title">{{this.title}}</h3>
                            <p class="blog-excerpt">{{this.summary}}</p>
                            <div class="blog-meta">
                                <span class="blog-date">{{this.formattedDate}}</span>
                            </div>
                        </div>
                    </div>
                    {{/each}}
                    
                    {{#if hasMoreBlogs}}
                    <button class="load-more-button" data-page="{{nextPage}}">Load More</button>
                    {{/if}}
                {{else}}
                    <p class="no-blogs-message">You haven't written any blogs yet. <a href="/write-blog">Start writing!</a></p>
                {{/if}}
            </div>
        </div>
    </main>

    {{> footer}}
    
    <!-- Include navigation script -->
    <script src="/js/nav.js"></script>
    <!-- Add profile specific script -->
    <script src="/js/profile.js"></script>
</body>
</html>
